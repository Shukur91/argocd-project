apiVersion: v1
kind: Service
metadata:
  name: {{ include "appliation.fullname" . }}
  labels:
    {{- include "appliation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appliation.selectorLabels" . | nindent 4 }}
