apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appliation.fullname" . }}-test-connection"
  labels:
    {{- include "appliation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appliation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
